# 🤖 Telegram Bot - Quick Start

## ✅ Что было сделано

Реализован полнофункциональный Telegram бот для сканирования активных чатов:

### 📁 Файлы проекта

```
thrhr/
├── bot.py                           # Основной скрипт бота
├── upload_to_gdrive.py             # Скрипт загрузки в Google Drive
├── run_bot.sh                      # Удобный launcher скрипт
├── requirements.txt                # Python зависимости
├── test_bot.py                     # Тесты
├── .env.example                    # Пример конфигурации
├── .gitignore                      # Игнорирование sensitive файлов
├── README.md                       # Основная документация
├── DEPLOYMENT.md                   # Детальное руководство
└── .github/workflows/telegram_bot.yml  # CI/CD автоматизация
```

### ✨ Возможности

1. **Сканирование активных чатов** - Получение списка всех активных чатов за N дней
2. **Детальная информация** - ID, название, тип, дата последнего сообщения, непрочитанные
3. **Экспорт в CSV** - Сохранение результатов в удобном формате
4. **Загрузка в Google Drive** - Автоматическая загрузка результатов
5. **GitHub Actions** - Автоматический запуск по расписанию
6. **Безопасность** - Маскирование sensitive данных, proper permissions

### 🔒 Безопасность

- ✅ Все sensitive данные замаскированы в логах
- ✅ Proper .gitignore для защиты credentials
- ✅ GitHub Actions с минимальными permissions
- ✅ CodeQL проверка пройдена (0 уязвимостей)
- ✅ No vulnerabilities в зависимостях

## 🚀 Как запустить бота

### Вариант 1: Локальный запуск (рекомендуется для первого раза)

1. **Получите Telegram API credentials:**
   - Откройте https://my.telegram.org
   - Войдите с номером телефона
   - Перейдите в "API development tools"
   - Создайте приложение и сохраните `api_id` и `api_hash`

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Настройте конфигурацию:**
   ```bash
   cp .env.example .env
   nano .env  # Отредактируйте и добавьте свои credentials
   ```

4. **Запустите бота:**
   ```bash
   ./run_bot.sh
   ```
   или
   ```bash
   python bot.py
   ```

5. **При первом запуске:**
   - Введите код подтверждения из Telegram
   - Создастся файл сессии (telegram_scanner.session)
   - При следующих запусках авторизация не нужна

### Вариант 2: Запуск через GitHub Actions

1. **Добавьте Secrets в GitHub:**
   - Перейдите: Settings → Secrets → Actions → New repository secret
   - Добавьте:
     - `TELEGRAM_API_ID`
     - `TELEGRAM_API_HASH`
     - `TELEGRAM_PHONE` (опционально)
     - `TELEGRAM_SESSION_BASE64` (после локальной авторизации)

2. **Создайте session file:**
   ```bash
   # Запустите бота локально один раз
   python bot.py
   
   # Закодируйте session в base64
   base64 telegram_scanner.session > session.txt
   
   # Добавьте содержимое session.txt как Secret TELEGRAM_SESSION_BASE64
   ```

3. **Запустите workflow:**
   - Автоматически: Каждый день в 00:00 UTC
   - Вручную: Actions → Telegram Bot Scanner → Run workflow

### Вариант 3: Интеграция с Google Drive

1. **Настройте Service Account:**
   - См. подробные инструкции в `DEPLOYMENT.md`

2. **Добавьте credentials:**
   ```bash
   export GDRIVE_SERVICE_ACCOUNT_JSON='{"type":"service_account",...}'
   export GDRIVE_FOLDER_ID='folder_id'
   ```

3. **Запустите полный цикл:**
   ```bash
   ./run_bot.sh  # Автоматически предложит загрузку в Drive
   ```

## 📊 Формат выходных данных

CSV файл `results.csv` с колонками:

```csv
chat_id,chat_name,chat_type,last_message_date,unread_count
-1001234567890,Tech News,channel,2026-01-05 10:30:00,0
1234567890,John Doe,user,2026-01-05 09:15:00,3
-987654321,Project Team,supergroup,2026-01-04 18:45:00,12
```

## 📚 Документация

- **README.md** - Основная документация с примерами
- **DEPLOYMENT.md** - Подробное руководство по развертыванию
- **test_bot.py** - Тесты (запуск: `python test_bot.py`)

## 🧪 Тесты

Все тесты пройдены успешно:

```bash
$ python test_bot.py
🧪 Запуск тестов бота...
============================================================
test_bot_can_be_imported ... ok
test_required_modules ... ok
test_requirements_file_exists ... ok
test_upload_script_exists ... ok
test_save_empty_results ... ok
test_env_example_exists ... ok
----------------------------------------------------------------------
Ran 6 tests in 0.126s
OK
```

## ⚠️ Важные замечания

1. **Первая авторизация:**
   - Требуется интерактивный ввод кода из Telegram
   - После создания session файла, авторизация не нужна
   - Session файл содержит токены доступа - храните в безопасности!

2. **GitHub Actions:**
   - Требуется предварительная локальная авторизация
   - Session файл нужно закодировать в base64 и добавить как Secret

3. **Безопасность:**
   - Никогда не коммитьте .env, .session, или JSON ключи
   - Используйте .gitignore (уже настроен)
   - Храните credentials как GitHub Secrets

4. **Rate Limits:**
   - Telegram может ограничить частоту запросов
   - При FloodWaitError подождите указанное время

## 🎉 Готово к использованию!

Бот полностью готов к запуску. Для начала:

1. Получите Telegram API credentials
2. Запустите `./run_bot.sh` или `python bot.py`
3. Введите код подтверждения
4. Наслаждайтесь результатами!

---

**Автор:** wafiko2005-prog  
**Лицензия:** MIT  
**Версия:** 1.0.0  
**Дата:** 2026-01-05
